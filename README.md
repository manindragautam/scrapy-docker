- before specifying WORKING_DIR add volumes in docker-compose.yml and run
  `docker-compose run web scrapy startproject tutorial`

- give full permissions to rad and write for tutorial
  `sudo chmod -R 777 tutorial`
